# Pull torch from the CPU-only PyPI mirror — otherwise sentence-transformers
# pulls full CUDA wheels (~2 GB total: torch + cuDNN + NCCL), which both
# blows up the image and breaks pip on the VPS via "no space left on device".
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# UI
gradio==5.50.0

# LLM
# langchain ≥1.0 with langchain-core ≥1.0 is the current major series.
# Upper bound on langchain-community was removed (was <0.4.0 in W16);
# it blocked pip resolver when langgraph/langchain-experimental needed
# newer langchain-community in W17. Pip resolver would backtrack for
# 30+ minutes on Docker build before giving up.
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# Loading + chunking
beautifulsoup4>=4.12.0
lxml>=5.0.0
langchain-text-splitters>=0.3.0

# Embeddings + vector store
langchain-huggingface>=0.1.0
langchain-qdrant>=0.2.0
sentence-transformers>=3.0.0
qdrant-client>=1.12.0

# Evaluation
ragas==0.2.15
datasets>=3.0.0
python-dotenv>=1.0.0

# Agent (W17)
langgraph>=0.2.0
langchain-experimental>=0.3.0
# ddgs is the rebrand of duckduckgo-search; current langchain-community
# DuckDuckGoSearchRun imports `from ddgs import DDGS`. Lower bound matches
# what resolves cleanly with langchain-community ≥0.3.20.
ddgs>=9.0.0
structlog>=24.0.0
