services:
  api:
    build: .
    image: task-api:latest
    container_name: task-api
    ports:
      - "8000:8000"
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      DEBUG: ${DEBUG:-false}
    restart: unless-stopped